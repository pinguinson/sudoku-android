<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
    </style>

    <style name="MainMenuButtonStyle" parent="@android:style/Widget.Button">
        <item name="android:textSize">22sp</item>
        <item name="android:textColor">@color/main_menu_text_color</item>
        <item name="android:gravity">center</item>
        <item name="android:background">@drawable/main_menu_button</item>
        <item name="android:padding">10dp</item>
        <item name="android:width">250dp</item>
    </style>

    <style name="ButtonGridMainStyle" parent="@android:style/Widget.Button">
        <item name="android:textSize">20sp</item>
        <item name="android:textColor">@color/main_menu_text_color</item>
        <item name="android:gravity">center</item>
        <item name="android:background">@drawable/in_game_button</item>
        <item name="android:layout_width">@dimen/box_size</item>
        <item name="android:layout_height">@dimen/box_size</item>
        <item name="android:layout_margin">5dp</item>
    </style>

    <style name="ButtonGridSmallStyle" parent="@android:style/Widget.Button">
        <item name="android:textSize">14sp</item>
        <item name="android:textColor">@color/main_menu_text_color</item>
        <item name="android:gravity">center</item>
        <item name="android:background">@drawable/in_game_button</item>
        <item name="android:layout_width">@dimen/box_size</item>
        <item name="android:layout_height">@dimen/box_size</item>
        <item name="android:layout_margin">5dp</item>
    </style>

</resources>
